#include "Header.h"

void decomposition(vector<float> A, vector<int> ai, int n, int m) //Разложение матрицы А
{
	vector<float> L;
	int sign = 1;
	float sum;
	/*for (int i = 0; i < n; i++)
	{
		i % 2 == 0 ? sign = -1 : sign = 1;
		L[i] = sqrt(sign*());
	}*/

	L[0] = sqrt(A[0]);

	for (int i = 1; i < n; i++)
	{
		for (int k = 0; k < i; k++) 
		{
			if 
			sum = 
		}
	}
}